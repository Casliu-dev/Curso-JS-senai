//Linguagem interpretada, não precisa compilar, roda direto no navegador
//node.js
//para o JS funcionar, precisa colocar a tag <script> no final do body do HTML
// , para que o HTML carregue primeiro e depois o JS. Se colocar no head
//  o JS vai tentar acessar elementos do HTML que ainda não foram carregados, dando erro.
//tipagem fraca

//Condicionais (if, else, operadores lógicos,)



//Exercício - Classificação da Temperatura
//Pede uma temperatura em °C e diz se está muito frio, frio, agradável, quente ou muito quente

//prompt() sempre devolve texto, então convertemos para número com Number()


const temp = Number(prompt("qual a temperatura?"))

if (number.isNaT(temp)) {
    alert("temperatura invalida")
} else if(temp <0) {

}

if (temp <0);
    alert("muito frio");

if (temp > 0 < 15);
    alert ("Frio");

if (temp > 16 < 15);
    alert ("agradavel");

if (temp > 26 < 35);
    alert ("");
 
if (temp > 35);
    alert ("muito quente");

    